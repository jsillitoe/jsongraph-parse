/**
 * Clean files and folders.
 *
 * https://www.npmjs.org/package/grunt-contrib-clean
 */

module.exports = {

    build: ["build/*"],  // clean all the files from the build directory

    dist: ["dist/*"] // clean all the files from the distribution directory

};
